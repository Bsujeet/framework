'use strict';

const Common = require('./component/common');
const Logger = require('./component/logger');
const MongoDB = require('./component/mongo');
const Server = require('./component/server');

module.exports = {
    Common, Logger, MongoDB, Server
};
